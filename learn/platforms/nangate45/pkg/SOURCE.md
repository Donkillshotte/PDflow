# Dummy bump LEF (educational)

`dummy_bump_gcd.lef` is a **scaled** `DUMMY_BUMP` master for the FlowLab GCD
die (88.8 µm). Geometry and CLASS follow OpenROAD
`src/pad/test/Nangate45_io/dummy_pads.lef` (BSD-3, The OpenROAD Project).

Honest leftover: dummy bump, not C4. Sidecar ODB only — never written into
`results/.../gcd/flowlab/6_final.odb`.
